#include <iostream>
#include <cmath>
using namespace std;

struct point {
    int x, y;
};

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    int T;
    
    cin >> T;
    
    while (T--) {
        double x1, x2, y1, y2;
        double x3, x4, y3, y4;
        
        cin >> x1 >> y1 >> x2 >> y2;
        cin >> x3 >> y3 >> x4 >> y4;
        
        
        cout << 1 << "\n";
    }
}
